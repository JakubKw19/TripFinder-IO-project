import { Planner } from "./Planner/Planner";

export const App: React.FC = () => {
    return (
        <div className="dark flex justify-center h-screen w-screen">
            <Planner />
        </div>
    );
};